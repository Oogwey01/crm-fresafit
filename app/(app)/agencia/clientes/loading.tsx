import { EsqueletoModulo } from "@/components/compartido/esqueleto";

/* La portada de clientes: tarjetas, una por empresa. */
export default function Cargando() {
  return <EsqueletoModulo tarjetas={3} filas={0} acciones={0} />;
}
