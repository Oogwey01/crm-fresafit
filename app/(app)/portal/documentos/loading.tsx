import { EsqueletoModulo } from "@/components/compartido/esqueleto";

/* El archivo de documentos: lista con buscador arriba. */
export default function Cargando() {
  return <EsqueletoModulo tarjetas={0} filas={8} acciones={1} />;
}
