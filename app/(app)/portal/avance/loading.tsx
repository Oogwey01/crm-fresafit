import { EsqueletoModulo } from "@/components/compartido/esqueleto";

/* El avance: bloques de texto, se aproxima con filas anchas. */
export default function Cargando() {
  return <EsqueletoModulo tarjetas={0} filas={6} acciones={1} />;
}
